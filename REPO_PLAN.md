# ai-team-intro repo plan

## Goal
Turn this folder into an independent GitHub repository without breaking existing files.

## Safe execution plan
1. Run `git init` inside this directory.
2. Commit the current prototype set as the baseline.
3. Push to `openclawsean024-create/ai-team-intro`.
4. Optional follow-up: move HTML demos into `apps/web/` and Python scripts into `apps/scripts/`.

## Why not auto-init right now?
The parent workspace is already a git repo, so nested repo creation should be an explicit repo-boundary action. The files are ready; the remaining step is isolated repo initialization/publish.
